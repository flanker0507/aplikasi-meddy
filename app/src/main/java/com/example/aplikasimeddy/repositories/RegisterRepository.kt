package com.example.aplikasimeddy.repositories

import com.example.aplikasimeddy.api.interfaces.UserApiService

class RegisterRepository(private val user: UserApiService) {
//    fun registerUser(body: UserRegister) = flow {
////        val response = user.validateEmail
//    }
}